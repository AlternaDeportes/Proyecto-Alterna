# Dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# Build
/.next/
/out/
/build

# Env — nunca commitear secretos
.env
.env*.local

# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript
next-env.d.ts
*.tsbuildinfo

# Vercel
.vercel

# IDE
.idea
.vscode

# OS
.DS_Store
Thumbs.db

# Referencia (prototipo estático, no forma parte del build)
# _referencia se mantiene en repo como documentación

# Documentos del proyecto académico (no van al deploy)
*.docx

# Media cruda descargada de Drive (staging local, no deploy)
/_media_drive/
